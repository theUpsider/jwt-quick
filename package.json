{
  "name": "jwt-quick",
  "version": "1.2.4",
  "description": "This is a TypeScript library for verifying JSON Web Tokens (JWTs) using JSON Web Key (JWK) sets.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "https://github.com/theUpsider/jwt-quick",
  "author": "David Fischer",
  "keywords": [
    "jwt",
    "jwks",
    "validate",
    "jwt url",
    "jwts url"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "publish": "yarn build && cp package.json dist/package.json && cp README.md dist/README.md && cd dist && npm publish && cd .. && rm dist/package.json"
  },
  "devDependencies": {
    "@jest/globals": "^29.5.0",
    "@types/node": "^18.11.9",
    "@types/node-fetch": "^2.6.2",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "rfc4648": "^1.5.2"
  }
}